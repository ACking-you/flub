function(target_if_include target path)
    if (TARGET ${target})
        message(WARNING "Target ${target} already exists!")
    else ()
        message(STATUS "Start fetch ${target},cmake path:${path}")
        include(${path})
    endif ()
endfunction()